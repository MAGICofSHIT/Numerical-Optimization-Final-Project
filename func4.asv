function f = func4(x, n)
% Extended Tridiagonal 2 function
% x: 输入向量
% n: 向量长度
% 返回值:
%   f: 标量函数值

    f = 0;
    for i = 1:n
        % f = f + 4 * (x(i)^2 - x(1))^2 + (x(i) - 1)^2;
        % f = f + 100 * (x(1) - x(i-1)^2)^2;
        f = f + 0.5 * (i * x(i)^2 - x(n));
    end
end